/** Custom protocol used to serve content from an opened .sitearchive. */
export const ARCHIVE_SITE_SCHEME = 'archive-site';

/** Session partition for viewing .sitearchive files (network fully blocked). */
export const SITEARCHIVE_PARTITION = 'sitearchive-viewer';
